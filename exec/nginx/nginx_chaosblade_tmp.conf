user www-data ;
worker_processes auto ;
pid /run/nginx.pid ;
events {
    worker_connections 768 ;
}
http {
    server {
        listen 80 default ;
        #nginx�����Ķ˿� server_name localhost ;
    }
}
location / {
    root /cpuis/front/ ;
    autoindex on ;
    index index.html ;
}
location /download {
    alias /cpuis/open/ ;
    #alias����ʾĿ¼ autoindex on ;
    a b ;
    c d ;
}
